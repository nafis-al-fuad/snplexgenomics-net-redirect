# SNPLex Genomics `.net` Redirect

Dedicated GitHub Pages site that redirects:

- `snplexgenomics.net`
- `www.snplexgenomics.net`

to the primary website at `https://snplexgenomics.com`.

The root page and 404 fallback preserve the requested path, query string, and URL fragment.